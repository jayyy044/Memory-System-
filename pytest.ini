[pytest]
testpaths = tests
norecursedirs = fixtures .venv .git
markers =
    live: invokes a real `claude` subprocess (nondeterministic, costs money/latency); excluded from the default run
addopts = -m "not live"
